name: 🎯 Download & Upload to Telegram v7

on:
  workflow_dispatch:
    inputs:
      url:
        description: '🎬 File URL'
        required: true
        type: string
      chat_id:
        description: '💬 Telegram Chat ID'
        required: true
        type: string
      caption:
        description: 'Optional caption'
        required: false
        type: string
        default: 'Downloaded file👌😎'

jobs:
  download-and-upload:
    name: 🚀 Download & Upload to Telegram
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repo (to get pre-built binary)
        uses: actions/checkout@v4

      - name: 🔧 Install runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl jq aria2 zip

      - name: ✅ Verify pre-built binary
        run: |
          if [ ! -f "bin/telegram-bot-api-bin" ]; then
            echo "❌ Pre-built binary not found at bin/telegram-bot-api-bin"
            echo "👉 Run the 'Build & Store Telegram Bot API Binary' workflow first"
            exit 1
          fi
          chmod +x bin/telegram-bot-api-bin
          echo "✅ Binary ready:"
          ./bin/telegram-bot-api-bin --version

      - name: 🤖 Start Local Telegram Bot API Server
        run: |
          mkdir -p bot-data

          ./bin/telegram-bot-api-bin \
            --api-id=${{ secrets.TELEGRAM_API_ID }} \
            --api-hash=${{ secrets.TELEGRAM_API_HASH }} \
            --local \
            --dir="bot-data" \
            --http-port=8081 \
            &

          echo "⏳ Waiting for local Bot API server..."
          READY=0
          for i in $(seq 1 30); do
            if curl -sf "http://127.0.0.1:8081/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/getMe" > /dev/null; then
              echo "✅ Bot API server is up!"
              READY=1
              break
            fi
            sleep 2
          done
          [ "$READY" -eq 1 ] || { echo "❌ Server failed to start"; exit 1; }

      - name: 📥 Download File
        env:
          URL: ${{ github.event.inputs.url }}
        run: |
          mkdir -p downloads tmp_downloads

          FILENAME=$(basename "$URL" | sed 's/[?#].*//')
          echo "⬇️ Downloading: $FILENAME"

          aria2c \
            --split=8 \
            --max-connection-per-server=8 \
            --min-split-size=20M \
            --dir="tmp_downloads" \
            --out="$FILENAME" \
            "$URL"

          LIMIT=$((1950 * 1024 * 1024))

          for FILE in tmp_downloads/*; do
            [ -f "$FILE" ] || continue
            SIZE=$(stat -c%s "$FILE")
            SIZE_MB=$(( SIZE / 1024 / 1024 ))
            BASENAME=$(basename "$FILE")

            if [ "$SIZE" -gt "$LIMIT" ]; then
              echo "✂️ ${BASENAME} is ${SIZE_MB}MB — splitting..."
              zip -9 -j -s 1950m "downloads/${BASENAME}.zip" "$FILE"
              echo "✅ Created $(ls downloads/ | wc -l) part(s)"
            else
              echo "✅ ${BASENAME} is ${SIZE_MB}MB — no split needed"
              cp "$FILE" "downloads/$BASENAME"
            fi
          done

          rm -rf tmp_downloads
          echo "📦 Files ready to upload:"
          ls -lh downloads/

      - name: 📤 Upload to Telegram
        run: |
          LOCAL_API="http://127.0.0.1:8081"
          BOT_TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}"
          CHAT_ID="${{ github.event.inputs.chat_id }}"
          CAPTION="${{ github.event.inputs.caption }}"

          upload_file() {
            local FILE="$1"
            local CAPTION_TEXT="$2"
            local FILENAME=$(basename "$FILE")
            local FILESIZE_MB=$(( $(stat -c%s "$FILE") / 1024 / 1024 ))

            echo "📤 Uploading: $FILENAME (${FILESIZE_MB}MB)"

            local RESPONSE HTTP_CODE
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
              "${LOCAL_API}/bot${BOT_TOKEN}/sendDocument" \
              -F "chat_id=${CHAT_ID}" \
              -F "document=@${FILE}" \
              -F "caption=${CAPTION_TEXT}" \
              --max-time 3600)

            HTTP_CODE=$(echo "$RESPONSE" | tail -n1)

            if [ "$HTTP_CODE" != "200" ]; then
              echo "❌ Failed: $FILENAME (HTTP $HTTP_CODE)"
              echo "$RESPONSE" | head -n-1
              curl -s -X POST "${LOCAL_API}/bot${BOT_TOKEN}/sendMessage" \
                -d chat_id="${CHAT_ID}" \
                -d text="❌ Failed to upload ${FILENAME} (HTTP ${HTTP_CODE})"
            else
              echo "✅ Uploaded: $FILENAME"
            fi

            sleep 1
          }

          TOTAL=$(ls downloads/ | wc -l)
          curl -s -X POST "${LOCAL_API}/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d text="🚀 Starting upload — ${TOTAL} file(s) queued..."

          for FILE in downloads/*; do
            [ -f "$FILE" ] || continue
            FILENAME=$(basename "$FILE")
            FILESIZE_MB=$(( $(stat -c%s "$FILE") / 1024 / 1024 ))
            upload_file "$FILE" "📁 ${FILENAME} | 📏 ${FILESIZE_MB}MB | ${CAPTION}"
          done

          if ls downloads/*.z[0-9]* 1>/dev/null 2>&1 || ls downloads/*.zip 1>/dev/null 2>&1; then
            curl -s -X POST "${LOCAL_API}/bot${BOT_TOKEN}/sendMessage" \
              -d chat_id="${CHAT_ID}" \
              -d text="📦 Split archive detected. Download all parts into the same folder and extract the .zip — 7-Zip, WinRAR, or unzip will join them automatically."
          fi

          curl -s -X POST "${LOCAL_API}/bot${BOT_TOKEN}/sendMessage" \
            -d chat_id="${CHAT_ID}" \
            -d text="✅ All uploads complete!"

      - name: 🧹 Cleanup
        if: always()
        run: |
          pkill telegram-bot-api-bin || true
          rm -rf downloads tmp_downloads bot-data
